FROM node:20-alpine

WORKDIR /app

# Copy package files first
COPY package*.json ./

# Install dependencies (using npm install to handle lock file updates)
RUN npm install

# Copy source code
COPY . .

# Default command runs tests
CMD ["npm", "run", "test"]

